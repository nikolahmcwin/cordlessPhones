/ * Read Me for E8 Cordless Phones */

1. Use Python 3
2. Run phones.py, passing input in a .txt file from stdin
3. The range found will be output as the diameter of the circle found (in metres)

Note: kdtree.py is a library used via https://pypi.org/project/kdtree/ however, we have made 3 minor edits to cast values returned. These edits are commented and dated to when we edited them.